export default function ClinicHeader() {
  return (
    <header className="cg-shell flex items-center justify-between py-6 sm:py-8">
      <span className="font-mono text-sm tracking-[0.02em] text-[color:var(--foreground)]">
        m.
      </span>
      <span className="cg-step-count">Clinic Growth — Private</span>
    </header>
  );
}
